# voiceforge-core

Shared Python package for the VoiceForge monorepo. It contains the relational data model, storage abstraction, audio pipeline contracts, backend registry, and job queue integration used by both the FastAPI API and the background worker.
