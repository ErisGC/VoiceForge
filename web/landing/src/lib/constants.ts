/** Price in COP (Colombian pesos) — single source of truth. */
export const PRICE_COP = 5_000;

/** Formatted price string for display. */
export const PRICE_DISPLAY = "$5.000 COP";

/** Wompi amount in cents (Wompi expects centavos). */
export const PRICE_WOMPI_CENTS = PRICE_COP * 100;

/** Product reference for payment tracking. */
export const PRODUCT_REFERENCE = "voiceforge-app-v1";

export const CONTACT_EMAIL = "soporte@voiceforge.app";

export const APP_NAME = "VoiceForge";
